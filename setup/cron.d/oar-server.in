#
# cron.d/oar-server -- schedules OAR accounting data mining
#

# By default, run every hours
0 * * * * %%ROOTUSER%% if [ ! -d /run/systemd/system ] && [ -x %%SBINDIR%%/oaraccounting ]; then %%SBINDIR%%/oaraccounting > /dev/null; fi

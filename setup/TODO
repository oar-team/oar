
- faire une passe sur les scripts init.d/logrotate/default/cron.d pour les templatifier.

- faire une passe sur les fichiers de conf

- trouver un moyen de différencier le process de postinstallation ?? (debian/tgz/rpm etc.) ('SETUP_DIST'=(tgz|debian|redhat|...)

- Mettre à jour INSTALL pour décrire les étapes d'installation 

- Faire un fichier UPGRADE pour décrire le passage de 2.4.x à 2.5.x



## server
  - shared
  - create the log
  - if tgz explain how to install init.d / default scripts

## user
  - shared

## node
  - shared
  - copy the ssh host keys if needed
  - if tgz explain how to install init.d / default scripts
  - if not already set explain how to setup oar ssh keys

## monika
  - shared
  - shared-www (link the apache-www.conf, if apache2 is used)

## draw-gantt
  - shared
  - shared-www
  - create /var/lib/drawgantt-files/cache
  - chown/chmod /etc/oar/drawgantt.conf

## doc
  - shared

## desktop-computing-agent
  - shared

## desktop-computing-cgi
  - shared
  - explain how to setup the apache conf for the cgi

## tools (admin)
  - shared

## api
  - shared
  - link the apache-api.conf file
  - enable apache2 modules ident and headers
  - reload apache2
  - [debian] suggest pidentd / oident

## poar
  - shared
  - link the apache2.conf file


## scheduler-ocaml
  - shared


## www-conf
  - shared
  - create the apache2.conf file

## libs
  - shared

## common (+ libs)
  - shared (check if started in a target environment, file owner/group, file rights)
  - create user oar
  - change shell for oar-user

## database
  - shared
  - check if database is accessible
    if yes, check the version and tell if the database need to be updated if needed.
    if no, tell how to setup the database.



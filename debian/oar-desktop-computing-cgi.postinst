#! /bin/sh
# postinst script for oar-desktop-computing-cgi
#
# see: dh_installdeb(1)

set -e
# summary of how this script can be called:
#        * <postinst> `configure' <most-recently-configured-version>
#        * <old-postinst> `abort-upgrade' <new version>
#        * <conflictor's-postinst> `abort-remove' `in-favour' <package>
#          <new-version>
#        * <deconfigured's-postinst> `abort-deconfigure' `in-favour'
#          <failed-install-package> <version> `removing'
#          <conflicting-package> <version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package
#

#set -x
#export DEBCONF_DEBUG=developer

. /usr/share/debconf/confmodule
db_version 2.0

adduser www-data oar || true

# TODO what does this do?
#perl -i -pe "s#^OARDIR=.*#OARDIR=/usr/lib/oar#;;s#^OARUSER=.*#OARUSER=oar#" /usr/lib/cgi-bin/oar-cgi

# Manage setuid bits
chown oar:oar /usr/sbin/oarcache
chmod a+s /usr/sbin/oarcache
chown oar:oar /usr/sbin/oarres
chmod a+s /usr/sbin/oarres
chown oar:oar /usr/lib/oar/oarres
chmod a+s /usr/lib/oar/oarres
chown oar:oar /usr/sbin/oar-cgi
chmod a+s /usr/sbin/oar-cgi

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0



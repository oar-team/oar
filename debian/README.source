Warning: do not use "gbp import-orig --uscan" until #796986 is fixed.
Else, the repack will be ignored. Instead, do it in two steps:
$ uscan
$ gbp import-orig ../picard-tools_[VERSION]+dfsg.1.orig.tar.gz

  -- Vincent Danjean <vdanjean@debian.org> 2015-09-21

global:
 distrib: debian
 debian_version_name: squeeze
 arch: amd64
 kernel_arch: "amd64"
 workdir_base: /var/tmp/kameleon
 distrib_repository: http://ftp.fr.debian.org/debian/
 extra_packages: "vim less bzip2 openssh-server rsync gnupg locales debian-keyring"
 #xen_domu_modules: /lib/modules/2.6.32-4-xen-amd64
 #bootstrap_fake_dir: /var/tmp/etch/chroot
steps:
 - debian_check_deps
 - check_deps:
   - rsync
   - building_appliance
   - building_kvm_images
# - bootstrap-fake
 - bootstrap
 - system_config
 - root_passwd
 - mount_proc
 - software_install:
   - extra_packages
 - kernel_install
 - strip
 - umount_proc
# - xen_domu
 - build_tgz
# - build_livecd
 - build_appliance:
   - create_raw_image
   - mount_image
   - copy_system_tree
   - install_grub
   - umount_image
   - save_as_raw
#   - save_as_vmdk
   - save_as_qcow2
#   - save_as_vdi
 - clean

